apiVersion: v1
kind: Pod
metadata:
  name: test-success
  namespace: default
  labels:
    test-scenario: success
    app: k8t-test
spec:
  containers:
  - name: nginx
    image: nginx:latest
    imagePullPolicy: IfNotPresent
  restartPolicy: Never
