apiVersion: v1
kind: Pod
metadata:
  name: test-auth-failure
  namespace: default
  labels:
    test-scenario: auth-failure
    app: k8t-test
spec:
  containers:
  - name: private-image
    # Image privée sans imagePullSecrets configuré
    # Remplacer par votre propre image privée si disponible
    image: registry.gitlab.com/private/nonexistent-private-image:latest
    imagePullPolicy: Always
  restartPolicy: Never
